// Single source of truth for the canonical site URL.
// Import this in any module that needs to construct absolute URLs
// (sitemap, robots, layout metadata, JSON-LD helpers, OG image route).

export const SITE_URL = "https://youraussieguides.com";
